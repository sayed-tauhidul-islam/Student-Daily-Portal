<x-app-layout>
    @php $isEdit = !is_null($school); @endphp
    <x-slot name="header"><div><p class="text-xs font-semibold uppercase tracking-[0.28em] text-cyan-300/80">Admin</p><h2 class="mt-2 text-3xl font-black text-slate-900 sm:text-4xl">{{ $isEdit ? 'Edit school' : 'Add school' }}</h2></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8"><form action="{{ $action }}" method="POST" class="space-y-4 rounded-[2rem] border border-slate-200 bg-white p-6 shadow-[0_18px_50px_rgba(15,23,42,0.08)]">@csrf @if($method !== 'POST') @method($method) @endif <input name="name" value="{{ old('name', $school?->name) }}" placeholder="Name" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5"><input name="area" value="{{ old('area', $school?->area) }}" placeholder="Area" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5"><input name="type" value="{{ old('type', $school?->type) }}" placeholder="Type" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5"><input name="rating" type="number" step="0.1" value="{{ old('rating', $school?->rating ?? 0) }}" placeholder="Rating" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5"><input name="students" type="number" value="{{ old('students', $school?->students ?? 0) }}" placeholder="Students" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3.5"><button class="rounded-2xl bg-slate-950 px-5 py-3.5 text-sm font-semibold text-white">Save</button></form></div></div>
</x-app-layout>
